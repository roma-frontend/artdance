# План реализации

Порядок работ от «клиент согласился» до продакшена. Задачи упорядочены по
зависимостям: следующая не начинается раньше, чем закрыта предыдущая в своей
цепочке. Отслеживание готовности компонентов — `npm run design:status`.

> Объём сверх макета — в `docs/07-feature-backlog.md`. Позиции с приоритетом **P0**
> входят внутрь задач этого плана (см. §7 бэклога) и должны учитываться **до**
> задачи 1.1: часть из них требует полей в схеме, а добавлять их после первой
> миграции дороже. Компоненты, которых нет в прототипе, — в
> `docs/08-component-catalog.md`; служебные модули — в `docs/09-helpers-catalog.md`.

## Шаг 0 — за один день до старта

Не требует согласия клиента, но требует его ответов.

| # | Задача | Блокирует |
|---|---|---|
| 0.1 | Получить ответы на открытые вопросы из `docs/03-conventions.md` §9 | цены, окна отмены, комиссии |
| 0.2 | Подтвердить акцент: бургунди `#8B1A2B` или красный `#6E1422` | вёрстку |
| 0.3 | Создать проект Supabase, получить `DATABASE_URL` и `DIRECT_DATABASE_URL` | 1.1 — **ждёт аккаунт заказчика.** Разработка идёт на локальном PostgreSQL 17 (`localhost:5432/artdance`), поставленном через winget; строки подключения в `.env.local`. Переезд на Supabase — замена двух переменных и `prisma migrate deploy` |
| 0.4 | Создать бакет R2, ключи доступа | 1.6 |
| 0.5 | Зарегистрировать `artdance.am`, подключить Cloudflare | деплой |
| 0.6 | Написать 2–3 банкам за условиями эквайринга (1–3 недели) | 5.x, **не** блокирует разработку |
| 0.7 | Начать регистрацию SMS-имени отправителя (2–3 недели) | уведомления |
| 0.8 | Передать юристу оферту, privacy, политику возврата | приём платежей |

## Фаза 1 — Foundation (2 недели)

| # | Задача | Зависит | Готово когда |
|---|---|---|---|
| 1.1 | Первая миграция: `npm run db:migrate` | 0.3 | ✅ сделано: две миграции (`init` — 59 моделей, 22 enum; `availability_resource_check` — семь CHECK-констрейнтов, которых Prisma не генерирует: ресурс расписания и владелец удержания «ровно один», цель отзыва «ровно одна», шкала оценки). `prisma migrate status` — up to date |
| 1.2 | `npm run design:import` + `npm run media:optimize` | — | ✅ сделано: 6,7 МБ → 0,94 МБ, `perf:budget` зелёный |
| 1.2b | Кодировать петли: `npm run video:encode -- --loop <петля>`, снять постер | 0.4 | ✅ сделано: обе петли в двух версиях кадра под ширину экрана, три формата на версию, бюджет 2,4 МБ на файл, постеры — первый кадр своей петли, автозапуск подавляется при reduced-motion и Save-Data |
| 1.3 | Компонент `Media` с presets и fallback | 1.2 | ни одного прямого `next/image` в проекте |
| 1.4 | Примитивы: `Badge`, `Price`, `RatingStars`, `SectionHeading`, `EmptyState` | 1.3 | все варианты из манифеста |
| 1.5 | `SiteHeader`, `MobileNavDrawer`, `SiteFooter`, `ThemeToggle` | 1.4 | тема переключается и сохраняется в cookie; мобильное меню с ловушкой фокуса и закрытием по Esc |
| 1.6 | Загрузка медиа: route handler + подписанные URL R2 | 0.4, 1.1 | 🔶 частично: `POST /api/media/upload` (`src/app/api/media/upload/route.ts`) — гвард `media.upload`, лимит частоты, `validateUpload` по всем kind, `processImage` (EXIF снят, ре-энкод AVIF/WebP, blur, бюджет пикселей), запись мастера и производных, `MediaAsset` с alt-текстом, аудит. Хранилище за интерфейсом `src/lib/media/storage.ts`: драйвер `local` (`public/media/uploads`) закрывает разработку целиком, драйвер `r2` **не реализован** — ключи создаются в аккаунте заказчика (0.4), а подписывать SigV4 без возможности проверить подпись значит получить неработающую загрузку на проде вместо понятной ошибки на старте. Прод падает с перечислением недостающих переменных |
| 1.7 | Аутентификация: Better Auth + сессии + lockout по `LoginAttempt` | 1.1 | ✅ сделано: `src/lib/auth/auth.ts` (Better Auth на Prisma-адаптере, схема приведена к именам библиотеки миграцией `better_auth_tables`), `/api/auth/[...all]`, `guards.ts` проверяет сессию через `auth.api.getSession` вместо прямого запроса к таблице, `lockout.ts` — блокировка по `LoginAttempt`. **Блокировка и наши лимиты частоты применяются в `hooks.before`/`hooks.after` библиотеки, а не в действии входа:** `/api/auth/sign-in/email` — публичный маршрут, и защита, живущая только в server action, его не касалась (`LoginAttempt` не получал ни одной строки при переборе через эндпоинт — нашла эта проверка). Свой ограничитель библиотеки отключён: он отвечал 429 после трёх запросов и перебивал наши числа. Проверено на живой сборке `npm run verify:auth` — 18 проверок, включая перебор напрямую и лимит частоты. Осталось: письма (нужен `RESEND_API_KEY`) |
| 1.8 | Экраны входа и регистрации + Turnstile | 1.7 | ✅ сделано: `/sign-in`, `/sign-up`, `/forgot-password`, `/reset-password` + минимальный кабинет `/account` (иначе успешный вход вёл в 404). Схемы форм — `src/domain/auth.ts`, действия — `src/server/actions/auth.ts`: капча, единый формат ошибок и отказ до лишней работы при блокировке. 12 e2e-проверок: вход, неверный пароль, неотличимость несуществующего адреса, блокировка после серии неудач, редирект с приватного раздела и возврат туда же, выход, регистрация, занятый адрес, вошедший не видит форм |
| 1.9 | `HeroSection` + `HeroVideo`, `StyleMarquee`, `EditorialStatement`, `Reveal` | 1.5, 1.2b | главная совпадает с макетом на 360 / 768 / 1441px, видео уважает reduced-motion |
| 1.10 | Сид демо-данными | 1.1 | ✅ сделано: `npm run db:seed` идемпотентен и сеет контент макета — 14 пользователей (админ, поддержка, клиент, 5 инструкторов, 3 владельца площадок, 3 автора отзывов), 5 профилей, 3 площадки с залами, 45 окон расписания, 4 занятия с 32 проведениями, 4 товара с 12 вариантами, 3 события, 3 отзыва, 19 медиа-ассетов с alt-текстами на трёх языках. Броней, заказов и платежей сид не создаёт: их создаёт продукт, проходя через гварды. Перед сидом убирается мусор проверок: пользователи с префиксами из `demoThrowawayPrefixes` на домене `demo.artdance.am`, их `LoginAttempt` и одноразовые токены — иначе `verify:auth` и e2e оставляют по аккаунту за запуск |

Итог фазы: главная страница совпадает с утверждённым дизайном, вход работает,
данные из макета в базе.

## Фаза 2 — Каталог (3 недели)

| # | Задача | Зависит |
|---|---|---|
| 2.1 | Слой запросов `src/server/queries/` с кешем и тегами | 1.1 | 🔶 в работе. Готово: `src/server/query.ts` — `defineQuery` (единственное место с примитивом кеша; теги из `cacheTags`, срок из `dataRevalidate`; `revalidate: 0` = без кеша; `Date` в аргументах отклоняется), 9 тестов. `queries/media.ts` — `MediaAsset` → `MediaRef` с переводами alt, размерами и blur (добавлена колонка `MediaAsset.focalPoint`; `Media` предпочитает данные манифесту сида и рисует заглушку роли иконкой на токенах вместо шести несуществующих SVG). `queries/reviews.ts` — один `select` и одно отображение отзыва на все четыре страницы. Из базы читают: **занятия** (день недели, время и свободные места из `ClassSession`; расписание — ближайшие 8 проведений), **инструкторы** (`specializations` из новой колонки, площадки через занятия, порядок — `domain/ranking.ts`, 11 тестов), **площадки** (цена «от» по залам, направление через занятия), **события** (прошедшее уходит из афиши по `endsAt`, но открывается по ссылке), **товары** (остаток за вычетом резерва, подарочная карта — из категории `commerce.giftCardCategorySlug`), **направления** (счётчики считаются, кадр — обложка занятия этого направления, `null` вместо чужой фотографии), **слаги каталога** (один источник для `generateStaticParams` и `sitemap.ts`). Отбор: индексируемые условия в SQL, текстовый запрос в приложении (ключ поиска умеет транслитерацию, `ILIKE` — нет; уедет в индекс с 2.8), потолок `limits.query.maxRows`. `EventCardItem.startsAt` стал ISO-строкой: кеш данных сериализует значения. Инварианты хаба переехали из юнит-тестов в e2e — юнит-тесты обязаны работать без базы. **CI: у джобов сборки и e2e появился сервис Postgres с миграциями и сидом** — без базы сборка каталога больше не проходит. Осталось: поиск, главная (её статистика и редакционный блок ждут `ContentBlock`, A-17), бронирование (3.2) и корзина (4.1) — читают фикстуры через тот же фасад `server/content/catalog.ts` |
| 2.2 | `ClassCard`, `InstructorCard`, `VenueCard`, `EventCard`, `ProductCard` | 1.4 |
| 2.3 | `StyleTileGrid`, `ClassCarousel`, `TestimonialCard` | 2.2 |
| 2.4 | Главная целиком: все секции макета | 2.3, 1.9 |
| 2.5 | `/discover` с фильтрами в URL и курсорной пагинацией | 2.2 |
| 2.6 | `/instructors`, `/studios`, `/events` — листинги | 2.2 |
| 2.7 | Страницы сущностей: занятие, инструктор, площадка, событие | 2.6 |
| 2.8 | `SearchOverlay` + `/api/search` | 2.5 |
| 2.9 | SEO: `generateMetadata`, JSON-LD, `sitemap.ts`, hreflang | 2.7 |
| 2.9a | ✅ сделано: A-01 хабы направлений (`/styles`, `/styles/[style]` — 18 × 3 локали, редакционный текст, noindex без предложения) и A-23 карточки для соцсетей (`opengraph-image` в каждом публичном сегменте) | 2.9 |
| 2.10 | Карта площадок (Google Maps) | 2.7 |

Итог: весь публичный каталог, индексируемый и кешируемый CDN.

## Фаза 3 — Бронирование (3 недели)

Самая дорогая часть. Каждая задача требует тестов.

| # | Задача | Зависит |
|---|---|---|
| 3.1 | ✅ сделано: движок доступности — `src/domain/availability/compute.ts` (правила → окна, праздники, исключения, буфер, сетка, горизонт), `src/lib/time/interval.ts`, `src/domain/holidays.ts`. Чистые функции, 87 тестов. Подключён к экрану бронирования через `src/server/content/booking.ts` | 2.1 |
| 3.2 | `SlotHold`: удержание, TTL, освобождение, уникальный индекс | 3.1 |
| 3.3 | ✅ сделано: `src/domain/availability/conflicts.ts` — конфликт ресурса, окно доступности, вместимость, правила платформы, двойная запись клиента. Уникальный индекс остаётся последней линией защиты и появится с 3.2 | 3.2 |
| 3.4 | `BookingCalendar`, `TimeSlotPicker`, `LocationOptionPicker`, `BookingSummary` | 3.3 |
| 3.5 | Создание брони: пересчёт цены на сервере, фиксация условий отмены | 3.4 |
| 3.6 | Отмена: окно, удержание, возврат — правила посчитаны в `src/domain/booking/policy.ts` (`cancellationOutcome`, `refundAmountFor`), остались server action и запись в БД | 3.5 |
| 3.7 | Перенос: лимит, повторная проверка конфликтов, разница в цене — правила посчитаны в `rescheduleOutcome`, остались server action и запись в БД | 3.5 |
| 3.8 | Лист ожидания: очередь, окно на подтверждение | 3.5 |
| 3.9 | Email-уведомления: подтверждение, напоминания, отмена | 3.5 |
| 3.10 | Cron: освобождение истёкших `SlotHold`, автозавершение, напоминания | 3.2 |

Итог: бронирование работает на mock-платежах, все сценарии покрыты тестами.

## Фаза 4 — Магазин (2,5 недели)

| # | Задача | Зависит |
|---|---|---|
| 4.1 | Корзина: клиентское состояние + серверный слепок, мерж гость/пользователь | 1.7 |
| 4.2 | `validateCart`: пересчёт по БД, diff для пользователя | 4.1 |
| 4.3 | Товар: варианты, остатки, галерея | 2.2 |
| 4.4 | `/checkout` — четыре шага | 4.2 |
| 4.5 | Заказ: пересчёт итогов на сервере, НДС, доставка | 4.4 |
| 4.6 | Промокоды: валидация и атомарное применение внутри заказа | 4.5 |
| 4.7 | Склад: списание, резерв, журнал `stockMovements` | 4.5 |
| 4.8 | Статусы заказа: единый переходник, идемпотентность | 4.7 |
| 4.9 | Подарочные карты | 4.5 |
| 4.10 | Отзывы и избранное | 2.7 |

## Фаза 5 — Платежи (1,5 недели, параллельно 3–4)

| # | Задача | Зависит |
|---|---|---|
| 5.1 | Адаптер выбранного провайдера по интерфейсу `PaymentProvider` | 0.6 |
| 5.2 | Webhook: подпись, дедупликация по `WebhookEvent`, никогда не 500 | 5.1 |
| 5.3 | Сверка статуса: подтверждение брони/заказа только после `getPayment()` | 5.2 |
| 5.4 | Возвраты: частичные, идемпотентность | 5.3 |
| 5.5 | Тестовые платежи с банком | 5.4 |

Разработка фаз 3–4 идёт на mock-провайдере и **не ждёт** договора с банком.

## Фаза 6 — Кабинеты (2,5 недели)

| # | Задача |
|---|---|
| 6.1 | Клиент: брони, заказы, избранное, отзывы, настройки, удаление аккаунта — обзор кабинета сделан вместе с 1.8 (пустые состояния, выход, роль). **Избранное сделано** (`/account/favorites`): иконка ♡ в шапке и плитка в мобильной шторке вели на 404, потому что страницы по объявленному адресу не было; список читает `Favorite` без кеша (персональные данные) и теми же условиями публичности, что каталог. Остальные разделы ждут броней и заказов |
| 6.2 | Инструктор: расписание, доступность, заявки, занятия, доходы |
| 6.3 | Площадка: залы, календарь, доходы |
| 6.4 | Уведомления: настройки каналов, история |

## Фаза 7 — Админка (2,5 недели)

Каркас реализован. Ключевое решение фазы: **четырнадцать разделов каталога
обслуживают три страницы** (`/admin/[resource]`, `.../new`, `.../[id]`), которые
читают описание из `src/config/admin.ts` — колонки, поля, права, вложенность.
Добавить раздел = добавить запись в описание и обработчик записи в реестре
(`src/server/admin/registry.ts`), а не написать ещё один экран. Экраны со своей
логикой (заказы, брони, выплаты, модерация, люди, права, аудит, отчёты) остались
отдельными: их нельзя свести к «список + форма».

| # | Задача | Состояние |
|---|---|---|
| 7.1 | Каркас: навигация по capability, `resolveCapabilities` | ✅ `src/app/[locale]/admin/layout.tsx` — сессия → роль ≥ SUPPORT → права одним запросом → фильтр меню. Сводка `/admin` считает только те показатели, на которые есть право. **По ходу закрыта дыра:** `hasCapability()` отвечал «разрешено» для роли вне персонала, то есть любой вошедший клиент прошёл бы `capabilityAction` |
| 7.2 | Каталог: занятия, инструкторы, площадки, товары, события | ✅ 14 ресурсов: занятия, проведения, инструкторы, площадки, залы, товары, категории, варианты, события, курсы, уроки, промокоды, подарочные карты, медиатека. Переводы (`ru`/`en`) вкладками в той же форме, пустой перевод удаляется, а не пишется пустой строкой |
| 7.3 | Заказы и брони: списки, фильтры, массовые действия с guardrails | ✅ Матрица переходов статусов на сервере; отмена брони считает удержание и возврат по условиям, **зафиксированным в самой броне**; массовые действия ограничены `maxBulkActionItems` |
| 7.4 | Пользователи и роли, матрица capability, временные гранты | ✅ Свой доступ менять нельзя, последнего активного администратора нельзя понизить или заблокировать; блокировка обрывает сессии; гранты не выдаются на деньги и роли |
| 7.5 | Модерация отзывов и профилей | ✅ Три очереди, причина отклонения обязательна и видна автору, рейтинги пересчитываются в транзакции с решением |
| 7.6 | Промокоды и подарочные карты | ✅ Как ресурсы реестра |
| 7.7 | Audit log с фильтрами и откатом | ✅ Фильтр по сущности, поиск по действию и почте; откат помечает запись и фиксирует намерение — автоматического восстановления значений нет намеренно: `diff` обезличен, и запись «[redacted]» обратно в БД была бы порчей данных |
| 7.8 | Отчёты и экспорт (через `toCsv`) | ✅ Оборот, брони, заказы, комиссия за 30/90/365 дней; комиссия — из `CommissionRecord` с зафиксированной ставкой, а не из текущего конфига; выгрузка заказов через `toCsv` |
| 7.9 | Maker-checker для `security.approvalRequiredActions` | 🔶 Ручной возврат выше `maxManualRefund` уходит в `ApprovalRequest`, раздел согласований работает (автор не одобряет свою заявку, просроченная не выполняется). **Одобрение даёт разрешение, но не исполняет операцию:** исполнение возврата требует адаптера банка (фаза 5) |
| 7.10 | Корзина: удаление в два шага | ✅ `deletedAt` в 13 моделях, уникальные ограничения переведены в частичные (`WHERE "deletedAt" IS NULL`), поэтому удалённая запись не занимает свой слаг. Удалённое не видно НИ ОДНОМУ чтению: расширение клиента (`src/lib/db.ts`) подмешивает фильтр в любой запрос верхнего уровня, вложенные связи несут `notTrashed` из `queries/relations.ts`, и это проверяет тест, читающий исходники. Раздел `/admin/trash`: восстановление (`trash.restore`) и окончательное удаление (`trash.purge`, невыдаваемое грантом). Удаление фото — прямо из формы владельца. Чистка по сроку (30 дней) — `POST /api/cron/purge-trash` с `CRON_SECRET`; файлы уходят из хранилища только при окончательном удалении |
| 7.11 | Загрузочные состояния | ✅ `Spinner`, `SkeletonCard/CardGrid/List/Table`, `LinkPending` (`useLinkStatus`), `loading.tsx` в `/admin` и `/account`, `<Suspense>` в `/discover`. Числа — в `motion.loading`. В корне локали `loading.tsx` не ставится: §2e конвенций |

Что осталось в фазе: e2e-проверки админки (8.4), драйвер R2 (1.6, ждёт 0.4),
исполнение одобренных заявок (после 5.1) и обучение заказчика (8.7).

## Фаза 8 — Запуск (1,5 недели)

| # | Задача |
|---|---|
| 8.1 | Мультиязычность: вычитка `hy` носителем, проверка `ru` и `en` |
| 8.2 | Правовые страницы от юриста |
| 8.3 | Lighthouse и Core Web Vitals: `lighthouserc.json` зелёный |
| 8.4 | E2E на Playwright: бронирование, покупка, оплата |
| 8.5 | Production-окружение: env, Redis для rate limit, Sentry, домен, SSL |
| 8.6 | Бэкапы и проверка восстановления |
| 8.7 | Обучение заказчика работе с админкой |
| 8.8 | Приёмка по чек-листу `deliveryPhases` |

---

## Итого

| Фаза | Длительность | Можно параллелить |
|---|---|---|
| 1 Foundation | 2 нед | — |
| 2 Каталог | 3 нед | с 5 |
| 3 Бронирование | 3 нед | с 5 |
| 4 Магазин | 2,5 нед | с 3 частично |
| 5 Платежи | 1,5 нед | с 2–4 |
| 6 Кабинеты | 2,5 нед | — |
| 7 Админка | 2,5 нед | с 6 |
| 8 Запуск | 1,5 нед | — |

При последовательной работе одного разработчика — около 18 недель;
с параллелизацией фаз 5 и 7 — **13–15 недель**, что укладывается в
3,5–4,5 месяца из коммерческого предложения.

## Правило завершения задачи

Задача закрыта, когда:

1. реализованы все состояния из `component-manifest.ts`;
2. `npm run verify` зелёный;
3. для денег, броней, комиссий и webhook — есть тесты;
4. `npm run design:status` не показывает потерянных классов макета;
5. экран проверен на 375px, 768px и 1440px в светлой и тёмной теме;
6. пройден чек-лист из `docs/10-polish-and-quality.md` §9.
