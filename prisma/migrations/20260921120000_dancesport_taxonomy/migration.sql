-- Таксономия DanceSport (требование заказчика от 21.09.2026).
--
-- Оба изменения — добавления значений в enum: существующие строки не
-- затрагиваются, откат не требуется (удаление значения из enum в PostgreSQL
-- невозможно без пересоздания типа, но оно и не понадобится: значения не
-- удаляются, а используются новыми разделами).

-- Социальная программа: кизомба перечислена заказчиком в ядре social events.
ALTER TYPE "DanceStyle" ADD VALUE 'KIZOMBA';

-- Афиша DanceSport: гала-концерты и показательные выступления.
ALTER TYPE "EventType" ADD VALUE 'CONCERT';
